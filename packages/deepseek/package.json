{
  "name": "@lemonwoo/deepseek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke:live": "pnpm run build && node scripts/live-smoke.mjs"
  },
  "dependencies": {
    "openai": "^5.22.0"
  }
}
